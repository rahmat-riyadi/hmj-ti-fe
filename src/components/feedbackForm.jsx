import { FormControl, InputLabel } from '@mui/material';

const FeedbackForm = () => {
    return (
        <FormControl>
            <InputLabel></InputLabel>
        </FormControl>
    );
}

export default FeedbackForm;